# 📖 دليل التثبيت والإعداد

هذا الدليل يشرح خطوة بخطوة كيفية تثبيت وإعداد بوت حذف الإعلانات المزعجة.

## المتطلبات الأساسية

قبل البدء، تأكد من توفر:

- **Python 3.8 أو أحدث**: [تحميل Python](https://www.python.org/downloads/)
- **pip**: مدير الحزم (يأتي مع Python عادة)
- **حساب تلقرام**: [تحميل تلقرام](https://telegram.org/)
- **اتصال إنترنت**: للتواصل مع خوادم تلقرام

## الخطوة 1: إنشاء البوت على تلقرام

### 1.1 التواصل مع BotFather

1. افتح تطبيق تلقرام
2. ابحث عن `@BotFather` وافتح المحادثة
3. أرسل الأمر: `/newbot`
4. اتبع التعليمات:
   - أدخل اسم البوت (مثل: "Spam Detector Bot")
   - أدخل اسم المستخدم (يجب أن ينتهي بـ bot، مثل: `spam_detector_bot`)

### 1.2 الحصول على رمز البوت

بعد إنشاء البوت، سيعطيك BotFather رمز الوصول (Token):

```
Here is your bot token:
123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefgh
```

**احفظ هذا الرمز في مكان آمن** - ستحتاجه لاحقاً.

## الخطوة 2: تحضير بيئة التطوير

### 2.1 استنساخ المشروع

```bash
# إذا كان لديك Git
git clone <repository-url>
cd telegram_spam_bot

# أو تحميل الملفات مباشرة
cd telegram_spam_bot
```

### 2.2 إنشاء بيئة افتراضية (اختياري لكن موصى به)

```bash
# على Windows
python -m venv venv
venv\Scripts\activate

# على macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

### 2.3 تثبيت المكتبات

```bash
pip install -r requirements.txt
```

## الخطوة 3: إعداد متغيرات البيئة

### 3.1 نسخ ملف الإعدادات

```bash
cp .env.example .env
```

### 3.2 تعديل ملف .env

افتح ملف `.env` وأضف:

```env
# رمز البوت من BotFather
TELEGRAM_BOT_TOKEN=123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefgh

# معرف المسؤول (اختياري)
TELEGRAM_ADMIN_ID=123456789

# إعدادات قاعدة البيانات
DATABASE_URL=sqlite:///./spam_bot.db

# إعدادات الكشف
DETECTION_SENSITIVITY=0.7
MAX_SIMILAR_MESSAGES=3
SIMILAR_MESSAGE_TIME_WINDOW=300
```

### كيفية الحصول على معرف المسؤول:

1. ابحث عن `@userinfobot` في تلقرام
2. أرسل أي رسالة
3. سيظهر معرفك الرقمي

## الخطوة 4: تشغيل البوت

### 4.1 التشغيل الأساسي

```bash
python main.py
```

يجب أن ترى رسالة مثل:
```
2025-11-27 18:40:00,000 - app.bot - INFO - 🚀 جاري بدء بوت حذف الإعلانات المزعجة...
2025-11-27 18:40:01,234 - app.bot - INFO - تم إنشاء قاعدة البيانات
2025-11-27 18:40:02,456 - app.bot - INFO - ✅ البوت يعمل بنجاح!
```

### 4.2 إضافة البوت إلى قروب

1. افتح قروب تلقرام
2. اضغط على اسم القروب
3. اختر "إضافة عضو"
4. ابحث عن البوت باسم المستخدم الذي أنشأته
5. أضفه إلى القروب

## الخطوة 5: التحقق من التشغيل

### 5.1 اختبار الأوامر الأساسية

في القروب، أرسل:

```
/start
```

يجب أن يرد البوت بـ:
```
👋 مرحباً بك في بوت حذف الإعلانات المزعجة!
```

### 5.2 اختبار الكشف

أرسل رسالة تحتوي على كلمات مفتاحية:
```
استشارة طبية - اتصل بنا على +966541904263
```

يجب أن يحذف البوت الرسالة تلقائياً.

## الخطوة 6: الإعدادات المتقدمة

### 6.1 تخصيص الحساسية

أرسل (إذا كنت مسؤول):
```
/sensitivity 0.8
```

### 6.2 إضافة كلمات مفتاحية مخصصة

```
/addkeyword كلمة_جديدة
```

### 6.3 عرض الإحصائيات

```
/stats
```

## استكشاف الأخطاء

### المشكلة: "TELEGRAM_BOT_TOKEN غير محدد"

**الحل:**
- تأكد من وجود ملف `.env`
- تأكد من إضافة الرمز الصحيح
- أعد تشغيل البوت

### المشكلة: "البوت لا يرد على الرسائل"

**الحل:**
- تأكد من أن البوت مسؤول في القروب
- تأكد من أن البوت لم يتم تعطيله (`/enable`)
- تحقق من السجلات للأخطاء

### المشكلة: "خطأ في قاعدة البيانات"

**الحل:**
- حذف ملف `spam_bot.db`
- أعد تشغيل البوت (سيتم إنشاء قاعدة جديدة)

### المشكلة: "الرسائل الشرعية يتم حذفها"

**الحل:**
- قلل حساسية الكشف: `/sensitivity 0.6`
- أضف المستخدمين الموثوقين إلى القائمة البيضاء: `/whitelist add @username`

## التشغيل المستمر (Production)

### استخدام Screen (Linux/macOS)

```bash
# بدء جلسة جديدة
screen -S telegram_bot

# تشغيل البوت
python main.py

# الخروج دون إيقاف: Ctrl+A ثم D
# العودة للجلسة: screen -r telegram_bot
```

### استخدام Systemd (Linux)

1. إنشاء ملف الخدمة:

```bash
sudo nano /etc/systemd/system/telegram-bot.service
```

2. أضف المحتوى:

```ini
[Unit]
Description=Telegram Spam Bot
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/home/ubuntu/telegram_spam_bot
ExecStart=/usr/bin/python3 /home/ubuntu/telegram_spam_bot/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

3. تفعيل الخدمة:

```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
```

## التحديثات والصيانة

### التحديث إلى أحدث إصدار

```bash
git pull origin main
pip install -r requirements.txt
```

### نسخ احتياطي من البيانات

```bash
cp spam_bot.db spam_bot.db.backup
```

## الخطوات التالية

بعد التثبيت الناجح:

1. اقرأ [README.md](README.md) للمزيد من المعلومات
2. استكشف الأوامر المتاحة
3. خصص الإعدادات حسب احتياجاتك
4. راقب السجلات والإحصائيات

## الدعم والمساعدة

إذا واجهت مشاكل:

1. تحقق من السجلات: `tail -f spam_bot.log`
2. اقرأ الأسئلة الشائعة
3. افتح Issue مع تفاصيل المشكلة

---

**نصيحة**: احفظ رمز البوت في مكان آمن ولا تشاركه مع أحد!
